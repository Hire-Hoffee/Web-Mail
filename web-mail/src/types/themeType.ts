type ThemeType = {
  colors: {
    body: string;
    header: string;
    selected: string;
    text: {
      primary: string;
      secondary: string;
    };
    logo: string;
  };
};

export default ThemeType;
